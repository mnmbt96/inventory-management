import { styled } from "@mui/material";
import { Link } from "react-router-dom";

export const CustomLink = styled(Link)({
  textDecoration: "none",
  color: "white",
  display: "block",
  margin: "2px 5px",
});
